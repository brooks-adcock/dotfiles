Read the project documentation before doing anything else.

## Instructions

1. Read all three files in `./docs/`:
   - `docs/architecture.md` — how the system works, directory structure, tech stack, data model
   - `decisions.md` — what was tried, what failed, what worked (newest first)
   - `status.md` — what's done, what's next, known issues, things we're NOT doing

2. When done, reply with exactly `10-8` and nothing else.

3. You are now ready to work. Do not re-discover things that are already documented. Do not retry approaches that are listed as failed in decisions.md. Do not suggest things listed under "Not Doing" in status.md.
